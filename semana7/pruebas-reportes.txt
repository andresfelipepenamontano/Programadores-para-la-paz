Al consultar la ruta GET /reportes después de hacer el primer POST, el servidor devuelve un arreglo JSON que contiene el primer reporte registrado con un ID asignado automáticamente por el servidor.
echo Al consultar la ruta GET /reportes después de hacer el primer POST, el servidor devuelve un arreglo JSON que contiene el primer reporte registrado con un ID asignado automáticamente por el servidor.
[
{
"id": 1,
"tipo": "Seguridad Vial",
"descripcion": "Falta de señalización y semáforo peatonal dañado en el cruce escolar"
}
]'
